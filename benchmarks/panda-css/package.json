{
  "name": "styled-static-bench-panda-css",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "panda codegen",
    "dev": "panda codegen && panda cssgen --minimal && vite",
    "build": "rm -rf ./dist && panda codegen && panda cssgen --minimal && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.11.4",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  },
  "overrides": {
    "ajv": "^8.18.0",
    "fast-uri": "^3.1.2",
    "picomatch": "^4.0.5"
  }
}
